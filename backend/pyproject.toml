[tool.pytest.ini_options]
addopts = "-q --maxfail=1 --cov=app --cov-report=term-missing"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
select = [
  "E","F","I","B","UP","SIM"
]
ignore = []
target-version = "py311"

[tool.black]
line-length = 100
target-version = ['py311']

